<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="cartMapper">
	<!-- 상품 추가 -->
	<insert id="addCart" parameterType="cart">
		INSERT INTO CART(CART_NO, PRODUCT_NO, PRODUCT_COUNT, MEMBER_ID)
			VALUES(SEQ_CART.NEXTVAL, #{productNo}, #{productCount}, #{memberId})
	</insert>

	<!-- 장바구니 목록 조회 -->
	<select id="cartList" resultType="_int">
		SELECT COUNT(*)
		FROM CART
	</select>
	
	<!-- 장바구니 총액 계산 -->
	<select id="sumMoney" resultType="_int">
		SELECT PRODUCT_COUNT * PRODUCT_PRICE
		FROM CART, PRODUCT
		WHERE PRODUCT_NO AND MEMBER_ID = #{memberId}
	</select>

	<!-- 장바구니 개별 상품 삭제 -->
	<delete id="deleteCart">
		DELETE FROM CART
		WHERE CART_NO = #{cartNo}
	</delete>

	<!-- 장바구니 비우기 -->
	<delete id="deleteAllCart">
		DELETE FROM CART
		WHERE MEMBER_ID = #{memberId}
	</delete>
	
	<!-- 장바구니 갱신 -->
	<update id="modifyCart">
		UPDATE CART
		SET PRODUCT_COUNT = #{productCount}
		WHERE CART_NO = #{cartNo}
	</update>
</mapper>